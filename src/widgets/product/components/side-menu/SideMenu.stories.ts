import { Meta, StoryObj } from '@storybook/react'
import SideMenu from './SideMenu'

type Story = StoryObj

const meta: Meta = {
  component: SideMenu,
}

export const SideMenuStory: Story = {
  name: 'widgets/SideMenu',
}

export default meta
