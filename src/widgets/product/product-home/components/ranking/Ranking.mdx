import {
  Canvas,
  Meta,
  Controls,
  Primary,
  Story,
  ColorPalette,
} from '@storybook/blocks'
import * as RankingItem from './RankingItem.stories'
import * as RankingExample from './Ranking.stories'
import Ranking from './Ranking'

<Meta of={RankingItem} />

export const Template = (args) => <Avatar {...args} />

# Ranking

메인 페이지에서 사용되는 랭킹 컴포넌트 입니다.

**❗️프로젝트 특성상 버튼의 사이즈가 부모 태그의 너비를 기반으로 하기 때문에 사이즈 속성은 없습니다.**<br/>
**❗️실제 사용 환경과 동일하게 하기위해 임의로 배경색 설정되어 있습니다. 실제 컴포넌트에는 배경색이 설정되어 있지 않습니다.**

# Example

<Primary />
<Controls />

# Example For Use

실제 랭킹아이템은 다음과 같이 카드 섹션에서 매핑됩니다.

<Canvas of={RankingExample.RankingExample} />
